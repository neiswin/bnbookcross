<% provide :page_title, "Главная" %>

<h1>Pages#index</h1>
<% if user_signed_in? %>
<p lead></p>

<p class="lead">
    <%= current_user.first_name %>
    <%= current_user.last_name %>
</p>
  
<% end %>
